{
  "name": "bffi-example-async",
  "version": "0.1.0",
  "private": true,
  "description": "Example bffi-rs native module: the async surface (Rust futures as JS Promises, cancellation, timeouts) consumed through the @z2net/bffi pipeline. @z2net/bffi resolves via the root tsconfig paths inside this monorepo; in a standalone project use `bun add @z2net/bffi`.",
  "license": "MIT",
  "type": "module"
}
