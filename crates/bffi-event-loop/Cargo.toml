[package]
name = "bffi-event-loop"
description = "Event loop abstraction for bffi-rs: a marshalling queue drained by run(), with pump() as a documented mock."
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
rust-version.workspace = true
readme = "README.md"

[lints]
workspace = true

[dependencies]
bffi-core = { path = "../bffi-core" }

[dev-dependencies]
bffi-callback = { path = "../bffi-callback" }
